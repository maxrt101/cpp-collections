#include <mrt/string.h>
#include <cstdio>

int main(int argc, char ** argv) {
  // mrt::String s = std::string("123abc");

  // mrt::Array<int> arr = {10, 25, 0, 16, -8, 7, 9};
  // mrt::Pair<int, mrt::String> p = {110, "ABC"};

  // printf("'%s'\n", s.c_str());
  // printf("%s\n", mrt::toString(arr).c_str());
  // printf("%s\n", mrt::String::format("({}, {})", p._1, p._2).c_str());
  // printf("%s\n", mrt::toString(p).c_str());

  // mrt::String s = "abcdef";

  // s.find("cd");

  // size_t start = 2, end = 5;
  // printf("'%s'[%zu:%zu] = '%s'", s.c_str(), start, end, s.substr(start, end).c_str());

  // mrt::String s = "abc cde 1234 ,,?";
  // auto result = s.split();

  // for (auto& token : result) {
  //   printf("'%s' ", token.c_str());
  // }
  // printf("\n");

  // mrt::String s = "suka pidar kurva suka gandon";
  // auto r = s.replace("suka", "blyad");
  // printf("'%s' -> '%s'\n", s.c_str(), r.c_str());
}
