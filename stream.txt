template <typename T>
struct Stream {
  void sink();
};

template <typename I, typename T> requires mrt::Iterable<I, T>
Stream<T> stream(I itr) {
  return {};
}

template <typename T>
struct Stream {
  void sink(F fn);
  Stream filter(F fn);
};


